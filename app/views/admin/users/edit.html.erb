<main>
  <div class="container text-center">
    <h1 class="my-10">Modifier l'utilisateur</h1>

    <%= form_with scope: :user, url: [:admin, @user], method: :put, multipart: true do |form| %>

      <div class="my-5">
        <%= form.label 'Rôle admin' %>
        <div class="my-5">
          <%= form.label 'Oui' %>
          <%= radio_button_tag :admin, true, @user.admin == true %>
        </div>
        <div>
          <%= form.label 'Non' %>
          <%= radio_button_tag :admin, false, @user.admin != true %>
        </div>
      </div>

      <div class="form-group text-center">
        <%= form.submit 'Modifier le profil', class:'btn' %>
      </div>
    <% end %>
  </div>
</main>